server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # 纯前端单页：任何路由都回到 index.html
  location / {
    try_files $uri $uri/ /index.html;
  }

  location ~* \.(?:js|css|svg|png|ico|json)$ {
    expires 7d;
    add_header Cache-Control "public";
  }
}
